/** Global search — Meilisearch via API */
export default function SearchPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold">Поиск</h2>
      <p className="mt-2 text-stone-600">Скелет — /api/v1/search?q=</p>
    </div>
  );
}
