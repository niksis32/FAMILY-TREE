# Copy to apps/web/.env.local (or set in root .env if your Next setup loads it).
# Canonical local URLs: docs/DOCKER_LOCAL_WINDOWS.md — web :3000, API :4000
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
